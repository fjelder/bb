{
  "name": "bb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tail": "npx tailwindcss -i ./src/css/input.css -o ./dist/style.css --watch",
    "serve": "browser-sync start --server dist --files \"dist/*.html, src/css/*.css, dist/*.css, dist/js/*.js\"",
    "start": "npm-run-all --parallel tail serve"
  },
  "author": "Fjelder",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  },
  "dependencies": {
    "browser-sync": "^2.28.1",
    "daisyui": "^2.51.3",
    "npm-run-all": "^4.1.5"
  }
}
